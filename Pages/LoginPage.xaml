<Page
    x:Class="App1.Pages.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel Width="300" Spacing="10">
            <TextBlock Text="Вход" FontSize="24" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <TextBox x:Name="UsernameTextBox" Header="Имя" PlaceholderText="Введите ваше имя"/>
            <PasswordBox x:Name="PasswordBox" Header="Пароль" PlaceholderText="Введите ваш пароль"/>
            <Button x:Name="LoginButton" Content="Войти" HorizontalAlignment="Stretch" Click="LoginButton_Click" Margin="0,20,0,0"/>
            <TextBlock x:Name="ErrorMessageTextBlock" Foreground="Red" TextWrapping="Wrap" HorizontalAlignment="Center" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Page>